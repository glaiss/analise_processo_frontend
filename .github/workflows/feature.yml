name: Frontend Feature CI

on:
  push:
    branches:
      - 'G-*'

permissions:
  contents: read

jobs:
  validate:
    name: Validate Build and Tests
    uses: ./.github/workflows/ci.yml
    with:
      node-version: '22'
